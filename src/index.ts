import * as validationConnection from './validationConnection';
import * as singleFileValidationRunner from './singleFileValidationRunner';

export import getValidationHostConnection = validationConnection.getValidationHostConnection;
export import runSingleFileValidator = singleFileValidationRunner.runSingleFileValidator;

