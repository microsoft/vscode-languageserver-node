{
	"name": "@vscode/languageserver-types",
	"description": "Types used by the Language server for node",
	"version": "3.18.0",
	"author": "Microsoft Corporation",
	"license": "MIT",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "https://github.com/Microsoft/vscode-languageserver-node.git",
		"directory": "types"
	},
	"bugs": {
		"url": "https://github.com/Microsoft/vscode-languageserver-node/issues"
	},
	"exports": {
		".": {
			"types": "./lib/main.d.ts",
			"default": "./lib/main.js"
		}
	},
	"scripts": {
		"prepublishOnly": "echo \"⛔ Can only publish from a secure pipeline ⛔\" && node ../build/npm/fail",
		"prepack": "npm run all:publish",
		"compile": "tsgo -b ./tsconfig.json",
		"clean": "rimraf lib",
		"watch": "tsgo -b ./tsconfig.watch.json -w",
		"lint": "eslint src",
		"test": "mocha",
		"all": "npm run clean && npm run compile && npm run lint && npm run test",
		"compile:publish": "tsgo -b ./tsconfig.publish.json",
		"all:publish": "git clean -xfd . && npm install --no-workspaces && npm run compile:publish && npm run lint && npm run test",
		"preversion": "npm test"
	}
}
