{
	"name": "testbed-server",
	"version": "0.1.0",
	"description": "TestBed Server",
	"private": true,
	"engines": {
		"node": "*"
	},
	"dependencies": {
		"vscode-uri": "^3.0.8"
	},
	"scripts": {
		"compile": "tsc -b ./tsconfig.json",
		"watch": "tsc --watch -b ./tsconfig.json"
	}
}
